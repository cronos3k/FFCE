# Rust build output
/target
/crate/target

# Deploy staging copies (duplicates of web/ and the built paper) — ignored.
# site/index.html (the landing page source) IS tracked, just not these copies.
/site/play/
/site/paper/

# Logs / scratch
*.log
*.err.log

# OS / editor cruft
.DS_Store
Thumbs.db
